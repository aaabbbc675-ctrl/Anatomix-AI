# TODO — بازبینی overflow رشته‌های A/B در Wave 2-3

## وضعیت فعلی: رشته‌های مازاد بر سقف A/B کاملاً کنار گذاشته می‌شوند (Commit 15)

`engine/talentId/file15_tierClassifier.js` طبق بخش ۱۶.۳ سند فقط ۳ رشته‌ی برتر A و ۵ رشته‌ی
برتر B را در خروجی `classifyTiers` نگه می‌دارد. سند برای رشته‌های مازاد (رتبه‌ی ۴+ در A، رتبه‌ی
۶+ در B) **هیچ سطلی تعریف نکرده** — برخلاف کلاس C که «بقیه» صریحاً `tier_C_low_potential`
می‌شود، برای A/B چنین سطل «بقیه»‌ای در type `TierClassification` (بخش ۱۶.۴ سند) نیست.

طبق تصمیم تاییدشده‌ی Commit 15 (گزینه‌ی الف): این رشته‌های مازاد کاملاً از خروجی `classifyTiers`
کنار گذاشته می‌شوند — **نه data loss واقعی**، چون MatchReport کامل هر رشته همچنان در خروجی
Commit 13 (`generateMatchReports`) در دسترس است؛ `classifyTiers` فقط یک لایه‌ی نمایشی/فیلترکننده
روی نمای پیش‌فرض داشبورد است، نه منبع حقیقتِ داده.

## قبل از Wave 2-3 (Commit 17-19، افزودن ۵۲ رشته) باید بازبینی شود

با فقط ۵ رشته‌ی فعلی، این محدودیت هرگز trigger نمی‌شود (حداکثر ۵ رشته باید بین ۴ سطل پخش
شوند). اما با ۵۲ رشته، کاملاً محتمل است که مثلاً ۸ رشته واقعاً tier A باشند و فقط ۳ تای برتر
نمایش داده شوند. باید بررسی شود:

1. آیا نیاز به یک UI جداگانه («مشاهده‌ی همه‌ی رشته‌ها»، نه فقط نمای پیش‌فرض tier-بندی‌شده) وجود
   دارد که به مربی امکان بدهد به MatchReportهای کامل (خارج از `classifyTiers`) دسترسی داشته باشد؟
2. آیا باید یک فیلد `overflow_sport_ids` (یا مشابه) به خروجی `classifyTiers` اضافه شود تا حداقل
   تعداد/شناسه‌ی رشته‌های کنارگذاشته‌شده معلوم باشد (بدون نمایش کامل MatchReport آن‌ها در نمای
   پیش‌فرض)؟

## تا این کار انجام نشود

با تعداد رشته‌ی زیاد (Wave 2-3)، مربی فقط ۳ رشته‌ی A و ۵ رشته‌ی B برتر را در `classifyTiers`
می‌بیند؛ برای دیدن بقیه‌ی رشته‌های A/B واجد شرایط، باید مستقیماً به خروجی کامل Commit 13
(`generateMatchReports`) مراجعه کند، نه به این فایل.
