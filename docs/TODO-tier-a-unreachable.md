# TODO — کلاس A فعلاً با داده‌ی واقعی موجود دست‌نیافتنی است

## ⚠️ ریشه — پیامد مستقیم یک TODO قدیمی، نه یک مشکل جدید

این یافته **مستقیماً از `docs/TODO-normative-data.md` (Commit 4) می‌آید**، نه یک
محدودیت جدید و مستقل: `engine/talentId/shared/normativeData.json` فقط دو تست
(`vertical_jump`, `sprint_10m`) × دو بازه‌ی سنی (`bio_age_10_11`,
`bio_age_14_15`) را پوشش می‌دهد، و **هیچ‌جا سطح `elite_top_5` را seed
نکرده** — فقط تا `excellent_top_20` (بونوس +۱۵ طبق `TIER_TO_PERF_BONUS` در
`file7_perfScoreCalculator.js`، نه +۲۵ که برای `elite_top_5` رزرو شده).

## کشف حین ساخت Commit 22 (تست‌های E2E)

حین طراحی `caseA_perfectMatch` (پروفایل ایده‌آل → انتظار کلاس A)، با محاسبه‌ی
دستیِ کامل زنجیره مشخص شد که **کلاس A (امتیاز نهایی ≥۸۵) با هیچ ترکیب واقعی
و فیزیولوژیک‌سازگاری از ورودی‌ها قابل‌دسترسی نیست**:

- سقف عملی perf_component: چون بالاترین tier موجود `excellent_top_20`
  (+۱۵) است، renormalize همیشه به دقیقاً `final_perf_score=115` می‌رسد —
  صرف‌نظر از این‌که چند تست ارزیابی شوند یا کدام رشته باشد.
- سقف عملی bio_component: بیشترین مجموع bonus قابل‌stack (بدون نیاز به
  `tall_stature`) در کل ماتریس ۵۲‌رشته‌ای `swimming_general` با ۶۰+ است
  (`ape_index_high+cormic_high+tbw_high+resting_hr_low`).
- بهترین حالت ممکن (وریفای‌شده با اجرای مستقیم pipeline، نه فقط محاسبه‌ی
  دستی): `swimming_general` + بلوغ دیررس (`late_maturer`, ضریب ×۱.۱۵) +
  پروفایل روانی کاملاً منطبق (تزریق‌شده، معادل مصاحبه‌ی موفق چت‌بات) →
  **`final_score = 83.25` (کلاس B)**، نه A.
- **یافته‌ی مهم‌تر**: `tall_stature` (بزرگ‌ترین bonusهای منفرد ماتریس،
  رشته‌هایی مثل `rowing`) با بلوغ دیررس (`late_maturer`) از نظر فرمول
  Mirwald **ذاتاً ناسازگارند** — جست‌وجوی عددی گسترده (سن ۱۰-۱۶، قد
  ۱۷۸-۲۰۰، وزن ۴۵-۹۰، هر دو جنس) صفر ترکیبی پیدا نکرد که هم‌زمان
  `standing_height_cm≥۱۹۰` و `maturity_type='late_maturer'` باشد — قدبلندی
  نسبت به سن خودش در فرمول Mirwald نشانه‌ی بلوغ زودرس است، نه دیررس.

این یافته منحصر به `caseA` نیست — `caseB_kyphosisExcluded` هم دقیقاً به
همین سقف برخورد کرد: بازیابی امتیاز بعد از اصلاح کایفوز نمی‌تواند از سقف
ریاضی فعلی سیستم (~۸۳.۲۵) فراتر برود، پس `what_if_analysis.estimated_tier_if_corrected`
در بهترین حالت `'B'` است، نه `'A'`. **این یک محدودیت سیستمی است، نه
مختص یک سناریو.**

## تصمیم تاییدشده (Commit 22)

به‌جای mock کردن `normativeData` (که تست‌ها را از حالت end-to-end واقعی
خارج می‌کرد)، سناریوهای E2E با همین سقف واقعی نوشته شدند —
`caseA_perfectMatch` و `caseB_kyphosisExcluded` هر دو صریحاً «نزدیک‌ترین
ممکن به A» (کلاس B) را هدف گرفته‌اند، با کامنت توضیحی در بالای هرکدام که
این یک محدودیت داده است، نه اشتباه در طراحی سناریو.

## اقدام لازم — وقتی normativeData واقعی جایگزین placeholder شد

طبق `docs/TODO-normative-data.md`: هر وقت یک Commit جداگانه دیتای واقعی
(شامل سطح `elite_top_5` و پوشش کامل ۱۰ تست × بازه‌های سنی بیشتر) را
جایگزین این placeholder کرد، **این TODO باید یادآوری کند که
`caseA_perfectMatch`/`caseB_kyphosisExcluded` در
`scripts/test-engine-talentid-e2e-scenarios.js` بازبینی شوند** تا واقعاً
کلاس A را (نه نزدیک‌ترین ممکن به آن را) تست کنند — اعداد hardcode‌شده‌ی
فعلی (۸۳.۲۵ و غیره) بعد از آن تغییر خواهند کرد.
