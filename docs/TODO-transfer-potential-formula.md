# TODO — بازبینی نگاشت transfer_potential در Wave 2/3

## وضعیت فعلی: نگاشت ساده‌ی ۳/۲/۱-دسته (Commit 14)

`engine/talentId/shared/sportSimilarityGraph.js` مقدار عددی `transfer_potential` را از
**تعداد دسته‌های هم‌پوشان** (`similar_by_anthropometry`/`similar_by_performance`/
`similar_by_psychology`، خودِ رشته با رشته‌ی هدف) با یک نگاشت ثابت و صریحاً placeholder
می‌سازد — نه یک فرمول علمی محاسبه‌شده:

- هم‌پوشانی در ۳ دسته → `0.90`
- هم‌پوشانی در ۲ دسته → `0.75`
- هم‌پوشانی در ۱ دسته → `0.60`

این تصمیم تاییدشده‌ی Commit 14 بود: سند (بخش ۱۵.۲) فقط نمونه‌ی عددی می‌دهد (مثلاً `rowing: 0.85`)
بدون هیچ فرمولی برای محاسبه‌اش — دقیقاً هم‌رده‌ی نمونه‌ی نمادین JSON بخش ۱۴.۵ که در Commit 13
کشف شد ناسازگار بود. به‌جای ساختن یک عدد دقیق جعلی، این قاعده‌ی شفاف و افشا‌شده انتخاب شد.

## قبل از استفاده‌ی production / با ورود رشته‌های Wave 2-3 باید بازبینی شود

با فقط ۵ رشته‌ی فعلی (Commit 1)، این granularity سه‌سطحی کافی است چون تعداد جفت‌های واقعی
بسیار کم است (فقط ۲ رابطه‌ی واقعی: `wrestling_freestyle↔weightlifting_olympic` و
`volleyball_middle_blocker→swimming_general`). اما با اضافه شدن ۵۲ رشته در Commit 17-19
(طبق نقشه‌ی راه سند)، این نگاشت ساده احتمالاً کافی نخواهد بود چون:

1. تعداد جفت‌های هم‌دسته به‌شدت زیاد می‌شود و همه در یکی از فقط ۳ سطح (۰.۹۰/۰.۷۵/۰.۶۰)
   جمع می‌شوند — تمایز کافی بین «خیلی شبیه» و «کمی شبیه» در همان دسته وجود نخواهد داشت.
2. باید بررسی شود آیا یک منبع علمی واقعی (مثل ادبیات Talent Transfer طبق UK Sport Sporting
   Giants / AIS، که بخش ۱۵.۱ سند به آن‌ها اشاره می‌کند) یک متریک شباهت کمّی واقعی (نه صرفاً
   شمارش دسته) ارائه می‌دهد که بشود جایگزین این placeholder کرد.

## تا این کار انجام نشود

اعداد `similarity_score` در `talent_transfer_suggestions` باید به‌عنوان یک رتبه‌بندی خام
(بالا/متوسط/پایین از نظر تعداد دسته‌ی هم‌پوشان) خوانده شوند، نه یک درصد شباهت علمی دقیق.
