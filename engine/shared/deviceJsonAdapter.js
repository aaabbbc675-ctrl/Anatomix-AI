// طبق بخش ۲ سند موتور اصلاحی («Schema فرضی دستگاه») و بخش ۱.۵ سند موتور
// بدنسازی: schema واقعی JSON خروجی سخت‌افزار دستگاه هنوز نهایی نشده. این
// آداپتور فعلاً فقط یک pass-through خالص است — ورودی را بدون هیچ تغییر،
// اعتبارسنجی یا فرض اضافه‌ای عیناً برمی‌گرداند، تا صادقانه بمانیم و هیچ
// فیلد فرضی اختراع نکنیم.
//
// وقتی schema نهایی دستگاه مشخص شد (زیرمرحله‌ی ۵.۷ موتور بدنسازی)، این فایل
// باید بازنویسی شود تا واقعاً فیلدهای مشخص را استخراج/اعتبارسنجی کند. هر دو
// موتور (بدنسازی و اصلاحی) باید از همین یک فایل مشترک استفاده کنند، نه
// نسخه‌ی موازی جدا.
function adaptDeviceJson(rawDeviceJson) {
  return rawDeviceJson;
}

export { adaptDeviceJson };
