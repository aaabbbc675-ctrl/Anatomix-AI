{
  "name": "anatomix-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Anatomix AI - engine-based coaching software (bodybuilding, corrective, hybrid, diet)",
  "main": "electron/main.js",
  "scripts": {
    "dev": "vite",
    "electron:dev": "concurrently -k \"cross-env BROWSER=none npm run dev\" \"wait-on http://localhost:5173 && cross-env ELECTRON_START_URL=http://localhost:5173 node scripts/launch-electron.js\"",
    "build": "vite build",
    "test:db": "cross-env ELECTRON_RUN_AS_NODE=1 electron scripts/test-db.js",
    "postinstall": "node scripts/rebuild-native-for-electron.js"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "electron": "^33.3.1",
    "vite": "^6.0.7",
    "wait-on": "^8.0.1"
  },
  "dependencies": {
    "@electron/rebuild": "^4.2.0",
    "better-sqlite3": "^12.11.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
