{
  "name": "anatomix-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Anatomix AI - engine-based coaching software (bodybuilding, corrective, hybrid, diet)",
  "main": "electron/main.js",
  "scripts": {
    "dev": "vite",
    "electron:dev": "concurrently -k \"cross-env BROWSER=none npm run dev\" \"wait-on http://localhost:5173 && cross-env ELECTRON_START_URL=http://localhost:5173 node scripts/launch-electron.js\"",
    "build": "vite build",
    "test:db": "node scripts/test-db.js"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "electron": "^33.3.1",
    "vite": "^6.0.7",
    "wait-on": "^8.0.1"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
